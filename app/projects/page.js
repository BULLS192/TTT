import Link from 'next/link';
import { projects } from '../../lib/siteData';
export const metadata={title:'Projects'};
export default function Page(){return <main><section className="page-hero"><div className="shell"><p className="eyebrow">Projects</p><h1>Every build becomes proof, documentation and knowledge.</h1><p className="lead">The project library is structured as a technical portfolio rather than a generic photo gallery. Real customer case studies will replace the launch templates as builds are completed.</p></div></section><section className="section"><div className="shell project-grid">{projects.map((p,i)=><article className="project-card" key={p.title}><div className="project-card__visual"><span>TTT / 00{i+1}</span></div><p className="eyebrow">{p.status}</p><h2>{p.vehicle}</h2><p>{p.title}</p><div className="tag-row">{p.tags.map(x=><span key={x}>{x}</span>)}</div></article>)}</div></section><section className="cta-band"><div className="shell"><h2>Your vehicle can become the next TTT project.</h2><Link className="button button--light" href="/start">Start a project →</Link></div></section></main>}
